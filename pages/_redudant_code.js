{
	/* <div className="flex">
					{cordinate_map.map((fence, idx) => {
						return (
							<div
								key={fence.name}
								id={fence.name}
								onMouseOver={setDisplayText}
								onMouseLeave={setDisplayText}
								style={{
									bottom: JSON.stringify(fence.y * (height / 1440)) + "px",
									left: fence.x,
									opacity: 0.9,
								}}
							>
								{getFire(
									JSON.stringify(fence.y * (height / 1440) + 40) + "px",
									fence.x,
									idx
								)}
							</div>
						);
					})}
					<img
						src="https://www.animatedimages.org/data/media/202/animated-dog-image-0503.gif"
						className="fixed"
						style={{
							bottom:
								JSON.stringify(cordinate_map[6].y * (height / 1440)) + "px",
							left: "78%",
							opacity: 0.85,
							filter: "brightness(0)",
						}}
					/>
				</div> */
}
